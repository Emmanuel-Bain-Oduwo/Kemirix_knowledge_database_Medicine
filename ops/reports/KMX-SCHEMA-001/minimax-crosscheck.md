PASS
Task-ID: KMX-SCHEMA-001
Head-SHA: 3b05c1c6f7ca8b948ba7cdba9ff6d959a8e048ff
Role: minimax

Findings: The implementation faithfully realizes the owner-approved blueprint for KMX-SCHEMA-001. The new `migrations/001_kmx.sql` creates exactly the five required tables (`kmx.registry`, `kmx.contains`, `kmx.external_identifier`, `kmx.name_index`, `kmx.mapping_exception`) under a single `kmx` schema, with no evidence/rules DDL, no data loading, and no source adapters. The registry enforces the `KMX-(ING|CD|PROD)-######` format and binds the prefix to the level via a dedicated CHECK constraint; status is constrained to `active`/`retired`; preferred_name and normalized_name are non-blank. The contains table uses the approved `container_kmx_id`/`member_kmx_id`/`relationship_type`/`ordinal` model with the composite PK, the `container <> member` CHECK, both FKs to registry, and a member lookup index — no parent/child wording or level columns leak in. The external_identifier table has the required identity PK, the single `UNIQUE (kmx_id, identifier_system, identifier_value)` binding, and crucially no global `UNIQUE (source_id, identifier_system, identifier_value)` — so identifier conflicts stay recordable and fail closed into `kmx.mapping_exception`. The name_index is a pure lookup aid (no UNIQUE, index on normalized_name, language defaults to `en`). The mapping_exception table carries full provenance: lane_id constrained to S01–S27, source_id constrained to the slug regex, source_version_key nullable, source_record_key NOT NULL, the five approved reason codes, jsonb normalized_input, jsonb candidate_kmx_ids defaulting to `[]`, status in `open`/`resolved`/`dismissed`, and a review-provenance CHECK that requires `reviewed_at` and `reviewed_by` when status is `resolved`. lane_id and source_id are kept as separate columns with separate CHECKs. `config/migration_suite.yaml` correctly promotes `001_kmx.sql` to `executable` while leaving 002/003 as `pending` placeholders. The new `tests/kmx/test_schema.py` and the updated `tests/test_runtime.py` assert the contracts concretely (regex patterns with valid/invalid examples, presence/absence of UNIQUE bindings, FK targets, index creation, migration-suite states, and independence from evidence/rules). The diff touches only allowed paths.

Checks:
- Verified `kmx.registry` columns: kmx_id PK, level, preferred_name, normalized_name, status, created_at, updated_at — all present with NOT NULL where required and the prefix/level/status CHECKs.
- Verified the kmx_id regex `^KMX-(ING|CD|PROD)-[0-9]{6}$` and the prefix-matches-level CHECK for all three levels.
- Verified `kmx.contains` uses container/member naming, composite PK, `container <> member` CHECK, both FKs to registry, member index, and contains no `parent_kmx_id`/`child_kmx_id`/`parent_level`/`child_level`.
- Verified `kmx.external_identifier` has exactly one UNIQUE constraint `(kmx_id, identifier_system, identifier_value)` and no `UNIQUE (source_id, …)` binding; source_id CHECK enforces the slug regex `^[a-z][a-z0-9_]*$` (rejecting S01–S27 style values).
- Verified `kmx.name_index` has identity PK, FK to registry, `language DEFAULT 'en'`, index on normalized_name, and no UNIQUE constraint.
- Verified `kmx.mapping_exception` has all required columns, lane_id CHECK `^S(0[1-9]|1[0-9]|2[0-7])$`, source_id slug CHECK, the five approved reason codes, status CHECK, and the review-provenance CHECK; no retired columns (`zero_match`, `multiple_match`, `resolution_kmx_id`, `resolved_at`).
- Verified migration 001 contains no references to `evidence.` or `rules.` schemas/tables and no `CREATE SCHEMA evidence`/`CREATE SCHEMA rules`.
- Verified `config/migration_suite.yaml` declares `001_kmx.sql` as `executable` and `002_evidence.sql`/`003_rules.sql` as `pending`.
- Verified the new tests assert the contracts with concrete valid/invalid examples for the kmx_id, lane_id, and source_id regexes, and explicitly check the absence of forbidden UNIQUE bindings and parent/child columns.
- Verified the diff only modifies allowed paths: `migrations/001_kmx.sql`, `migrations/README.md`, `config/migration_suite.yaml`, `tests/test_runtime.py`, `tests/kmx/test_schema.py` (new), `src/kmx/README.md`. No data loading, no evidence/rules DDL, no source adapters, no engineering-system changes.
