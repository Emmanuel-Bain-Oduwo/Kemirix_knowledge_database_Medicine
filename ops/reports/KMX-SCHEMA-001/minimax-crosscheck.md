PASS
Task-ID: KMX-SCHEMA-001
Head-SHA: ac2a27bb68d2d54271ee86b37f3bd09a7db9e1c0
Role: minimax

Findings: The implementation faithfully realizes the owner-approved blueprint for KMX-SCHEMA-001. The five tables (registry, contains, external_identifier, name_index, mapping_exception) are created in `migrations/001_kmx.sql` with the exact column sets, controlled vocabularies, and constraints specified. The registry enforces the KMX-(ING|CD|PROD)-###### format and binds level to the prefix via a CHECK constraint. The contains table uses container/member naming with the required composite PK, self-check, and FKs to registry, with no parent/child wording or level columns. The external_identifier table has only the (kmx_id, identifier_system, identifier_value) UNIQUE binding — no global (source_id, …) unique — so identifier conflicts stay recordable and fail closed into mapping_exception. The name_index is a pure lookup aid with no UNIQUE constraint and an index on normalized_name. The mapping_exception table carries full provenance (lane_id S01–S27, source_id slug, nullable source_version_key, NOT NULL source_record_key, the five reason codes, jsonb normalized_input, jsonb candidate_kmx_ids defaulting to '[]', and the open/resolved/dismissed status with a review-provenance CHECK). Migration 001 has zero references to evidence or rules schemas. The migration_suite.yaml correctly promotes 001 to executable while 002/003 remain pending. The new tests/kmx/test_schema.py and the updated tests/test_runtime.py assert every contract: suite plan, placeholder discipline, database.yaml inventory match, migration safety (no DROP/GRANT/ALTER SYSTEM/COPY/EXTENSION, only CREATE/COMMENT ON), regex patterns for kmx_id, lane_id, and source_id with positive and negative examples, level/prefix binding, container/member model, no global external-identifier unique, name_index lookup-only semantics, full mapping_exception provenance, and independence from future Evidence tables. All changes are within the allowed paths.

Checks: Verified the SQL contains CREATE SCHEMA kmx and exactly the five CREATE TABLE statements matching config/database.yaml inventory. Verified the registry CHECK constraints: kmx_id regex `^KMX-(ING|CD|PROD)-[0-9]{6}$`, level IN ('ingredient','clinical_drug','product'), prefix-matches-level CHECK, status IN ('active','retired'), and non-blank preferred_name/normalized_name. Verified contains has container_kmx_id, member_kmx_id, relationship_type, ordinal, composite PK, self CHECK, both FKs to registry, and the member index; confirmed no parent_kmx_id/child_kmx_id/parent_level/child_level anywhere. Verified external_identifier has only UNIQUE (kmx_id, identifier_system, identifier_value), no UNIQUE involving source_id, source_id regex `^[a-z][a-z0-9_]*$` (rejects S06, DailyMed, dailymed-, 6dailymed, "daily med"), and jurisdiction/active columns. Verified name_index has identity PK, kmx_id FK, normalized_name, name_type, language DEFAULT 'en', source_id slug, created_at, index on normalized_name, and no UNIQUE. Verified mapping_exception has all required columns, lane_id regex `^S(0[1-9]|1[0-9]|2[0-7])$` (accepts S01/S06/S15/S27, rejects S00/S28/S6/S001/s06/dailymed), the five reason codes, status IN ('open','resolved','dismissed'), DEFAULT '[]'::jsonb, and the review-provenance CHECK; confirmed retired names (zero_match, multiple_match, resolution_kmx_id, resolved_at) are absent. Verified migration 001 contains no "evidence.", "rules.", "CREATE SCHEMA evidence", "CREATE SCHEMA rules", or "REFERENCES evidence/rules". Verified config/migration_suite.yaml declares 001 executable and 002/003 pending. Verified tests/test_runtime.py was updated to assert the new executable prefix and pending tail. Verified all diff paths are within the allowed set (migrations/001_kmx.sql, migrations/README.md, config/migration_suite.yaml, tests/test_runtime.py, tests/kmx/test_schema.py, src/kmx/README.md).
