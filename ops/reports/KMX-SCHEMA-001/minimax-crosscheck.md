PASS
Task-ID: KMX-SCHEMA-001
Head-SHA: 61d8b7490fb4c69f87badb56116de6248cf99a14
Role: minimax

Findings: The implementation faithfully realizes the owner-approved blueprint for KMX-SCHEMA-001. The migration creates exactly the five required tables (`kmx.registry`, `kmx.contains`, `kmx.external_identifier`, `kmx.name_index`, `kmx.mapping_exception`) with the correct columns, controlled vocabularies, and constraints. The KMX id format `^KMX-(ING|CD|PROD)-[0-9]{6}$` is enforced and bound to the `level` column via the prefix-matches-level CHECK. The `kmx.contains` table uses the `container_kmx_id`/`member_kmx_id`/`relationship_type`/`ordinal` model with the composite PK and the not-self CHECK, and contains no parent/child or level columns. `kmx.external_identifier` has only the `(kmx_id, identifier_system, identifier_value)` UNIQUE binding — no global `(source_id, identifier_system, identifier_value)` UNIQUE — so identifier conflicts stay recordable and fail closed into `kmx.mapping_exception`. The `source_id` slug regex `^[a-z][a-z0-9_]*$` correctly rejects S01-S27 lane values. `kmx.name_index` is a pure lookup aid with no UNIQUE constraint and an index on `normalized_name`. `kmx.mapping_exception` carries full provenance (lane_id S01-S27, source_id slug, nullable source_version_key, NOT NULL source_record_key, the five reason codes, jsonb normalized_input, jsonb candidate_kmx_ids default `'[]'::jsonb`, status open/resolved/dismissed, reviewed_at/reviewed_by with the resolved-provenance CHECK) and has zero references to evidence/rules schemas. The migration suite is correctly promoted (001 executable, 002/003 pending), and the new `tests/kmx/test_schema.py` plus the updated `tests/test_runtime.py` assert every contract: suite plan, placeholder status of later migrations, DDL/inventory parity, migration safety (no DROP/CREATE DATABASE/CREATE EXTENSION/GRANT/ALTER SYSTEM/COPY), KMX id regex, registry level/prefix binding, contains container/member model, external-identifier recordability, name_index lookup-only semantics, mapping_exception provenance, independence from evidence tables, and config/migration_suite.yaml declaration. All edits stay within the allowed paths.

Checks: Verified `migrations/001_kmx.sql` contains `CREATE SCHEMA kmx;` and exactly five `CREATE TABLE kmx.*` statements matching `config/database.yaml` inventory.
- Verified `kmx.registry` columns (kmx_id, level, preferred_name, normalized_name, status, created_at, updated_at), PK on kmx_id, KMX id regex CHECK, level IN ('ingredient','clinical_drug','product'), prefix-matches-level CHECK, status IN ('active','retired'), and no UNIQUE constraint.
- Verified `kmx.contains` uses container_kmx_id/member_kmx_id/relationship_type/ordinal, composite PK, not-self CHECK, both FKs to registry, member index, and absence of parent/child/level columns.
- Verified `kmx.external_identifier` has UNIQUE only on `(kmx_id, identifier_system, identifier_value)`, no UNIQUE involving source_id, source_id regex `^[a-z][a-z0-9_]*$` (rejects S06, DailyMed, etc.), and required columns (jurisdiction, active, created_at).
- Verified `kmx.name_index` has identity PK, FK to registry, language DEFAULT 'en', source_id slug CHECK, index on normalized_name, and no UNIQUE constraint.
- Verified `kmx.mapping_exception` has all required columns, lane_id regex `^S(0[1-9]|1[0-9]|2[0-7])$`, source_id slug regex, all five reason codes, status CHECK, resolved-provenance CHECK, and no references to evidence/rules.
- Verified migration 001 has no `evidence.`, `rules.`, `CREATE SCHEMA evidence`, `CREATE SCHEMA rules`, or `REFERENCES evidence/rules` strings.
- Verified `config/migration_suite.yaml` declares 001 as executable and 002/003 as pending.
- Verified `tests/kmx/test_schema.py` and updated `tests/test_runtime.py` assert the suite plan, placeholder status, DDL/inventory parity, migration safety, regex patterns (with valid/invalid samples), level/prefix binding, container/member model, external-identifier recordability, name_index lookup-only semantics, mapping_exception provenance, evidence independence, and config declaration.
- Verified all modified files are within the allowed paths (migrations/001_kmx.sql, migrations/README.md, config/migration_suite.yaml, tests/test_runtime.py, tests/kmx/test_schema.py, src/kmx/README.md).
