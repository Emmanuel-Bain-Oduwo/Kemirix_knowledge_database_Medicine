PASS
Task-ID: S01-RXNORM-001
Head-SHA: ab6bdfda7ee066fac2af53bbc06be44ab7479a9b
Role: minimax

1: OK - release.py parses the verified-live JSON list shape, requires exactly one current RxNorm Full Monthly Release, validates product/type, and verify_pinned fails closed with PINNED_RELEASE_MISMATCH on filename/version/URL drift; pin constants match the directive exactly (RxNorm_full_09082026.zip, 2026-09-08, MD5 34dd95b0ae128fb81bc68166944514f2, official download URL); RxNorm_full_current.zip is never referenced.
2: OK - download.py reads UTS_API_KEY from the environment only, builds the authenticated URL with the key as a query parameter (never logged or echoed in errors), streams through the shared Phase 4 SourceHttpClient with NLM cap (2.0 rps, policy_host) and inherited 429/Retry-After, verifies the official pinned MD5 before SHA-256, and fails closed when the key is absent.
3: OK - rrf.py streams pipe-delimited UTF-8 members located by case-insensitive basename anywhere in the ZIP with the official column layouts (RXNCONSO 18, RXNREL 16, RXNSAT 13), applies SAB=RXNORM and SUPPRESS=N filters, and yields line-by-line with no unbounded buffering.
4: OK - loader.py implements deterministic passes with the official RXCUI2 RELA RXCUI1 direction; IN anchors mint KMX-ING; PINs mint their own identities and link to the base only through proven form_of (unproven keeps identity without a link); SCDs mint brand-neutral KMX-CD with ingredient members from consists_of+has_ingredient/has_precise_ingredient plus has_ingredients+has_part for combinations, deterministic ordinals, exact RXCUI identifiers, SUPPRESS=N only, and CVF=4096 prescribable tracking; SBDs project brand aliases onto existing CDs only through proven tradename_of and never mint PROD; unresolved structures record mapping exceptions with full provenance; reruns are idempotent.
5: OK - adapter.py wires discovery through the frozen order (raw first, manifest LAST) with the parser reading the stored copy; the offline test suite proves the whole flow with fake HTTP (real discovery payload shape), a synthetic RRF ZIP, the fake vault and the fake repository/builder stack, plus pin-drift and MD5-mismatch fail-closed tests; no live network access; only src/sources/rxnorm and tests/test_rxnorm.py changed; no Evidence/Rule work, no other lane adapters, no changes to frozen contracts.
Findings: All five modules conform to the directive: pin constants match exactly, the official relation direction is honored, SBDs never mint PROD, and the adapter enforces vault order (raw first, manifest LAST) with the parser reading the stored copy. The offline test suite covers the full pinned flow plus both fail-closed paths with no live network access.
Checks: The diff is restricted to src/sources/rxnorm and tests/test_rxnorm.py; no frozen contracts, Evidence/Rule work, or other lane adapters are touched. The fake HTTP transport, fake vault, and fake repository/builder stack exercise every requirement end-to-end.
