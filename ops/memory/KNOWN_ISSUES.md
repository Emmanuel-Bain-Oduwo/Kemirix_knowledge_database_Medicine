# Known issues and remaining gates

- RESOLVED (FOUNDATION-CD-AND-SKILL-001): the development migration execution is implemented and the Deploy development workflow is green again (exact-SHA deployments succeed including executable migrations; the dev database carries the applied kmx schema). Multi-migration upgrades (002/003) still require the DATABASE-001 migration runner and are refused until it exists.
- The legacy `develop` branch still exists in local/remote refs. The owner must delete it in GitHub once main-only operation is confirmed; tooling and documentation no longer reference it.
- GitHub auto-merge feature enablement and automatic head-branch deletion remain owner-side. Main protection IS active (protect-main ruleset: squash-only, PR required, linear history, no force pushes, required checks foundation + kemirix-agent-gate). The ruleset's required-check contexts were corrected 2026-09-08 from an accidental single joined-context entry to the two documented separate contexts (D021).
- Codex CLI login is functional (ChatGPT-authenticated); an inference smoke remains structurally validated only. Requested model availability, account entitlements and endpoint compatibility for the remaining roles were exercised by real runs (Nebius MiniMax-M3, Nebius Nemotron 3 Ultra, Cloudflare GLM 5.3, Nebius Kimi K3) but are not continuous guarantees. Use the protected EnvironmentFile/operator wrapper method, never loosen /etc/kemirix/agents.env permissions.
- Cloudflare Workers AI wall clock cannot host GLM review-sized contexts (HTTP 408 at ~80KB+); GLM architecture reviews run through the OpenCode GLM harness with full repository read access instead of a single large Cloudflare API call.
- Gated writes/submit enforce ownership, but unrestricted shells under one Unix account can bypass them. Operators must restrict reviewer tools/OS write access, protect governance/runtime paths and stop the old writer before handoff. Human approval flags are attestations, not authentication.
- External review reports (ops/reports/<task>) remain local control-checkout evidence; documented exception: KMX-SCHEMA-001's glm-review.md was also committed on its task branch through the authorized writer-report path and merged to main as durable audit evidence (stale binding lines document the checkpoint progression). One control VM is supported; the file lock is not distributed coordination.
- The writer seat is glm under the owner's 2026-09-08 directive (writer and review seats executed by the GLM 5.3 OpenCode harness; minimax/nemotron seats remain real independent Nebius provider runs; human-attestation flags record the owner's explicit directives, D020).
- DBeaver verification pending. Existing documentation describes usage but does not confirm inspection.
- RESOLVED (DOMAIN-CONTRACT-001 + SKILL v5.0 sync): lane_id vs source_id is frozen, all 27 configs carry stable slugs, config/database.yaml now inventories the full 6-table evidence contract including source_version and block_subject, and the repository SKILL.md is byte-identical to the owner-approved v5.0 blueprint.
- Source rights/access registry and rate limits remain unverified contracts. Source integrations remain not_started.
- Runtime pointer updates are individually atomic; a crash between them produces detectable mismatch requiring operator reconciliation. Partial failed releases are preserved for explicit quarantine. OS/interpreter integrity and a compromised deploy account are outside the release hash guarantee.
- Search backend is intentionally disabled. Existing safe agent web/MCP tools may be used only when already approved; no new paid service/credential was introduced.
- The local sandbox shows a bubblewrap warning during sandboxed CLI execution; it remained non-fatal in observed runs.
