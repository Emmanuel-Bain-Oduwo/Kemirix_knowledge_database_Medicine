# Known issues and remaining gates

- Migrations are placeholders: all three unchanged SQL files are explicitly pending/non-executable. No schema or clinical correctness claim follows from a foundation CI pass.
- PR creation and automatic squash merge have executed successfully (owner-confirmed). Main protection, required-check configuration, auto-delete head branches and old branch deletion remain owner-side GitHub configuration after this checkpoint; code does not change these settings.
- Legacy branch cleanup remains owner-side. Current remote branch inventory was not rechecked in this closeout.
- Main CI has executed successfully (owner-confirmed). The ordinary `foundation` check does not prove independent agent review. `agents.publish_gate` has now been externally invoked (PR #3, exact head db709d7, `kemirix-agent-gate` success) and the closeout's full external review chain executed against real providers; the owner must still require both `foundation` and `kemirix-agent-gate` in GitHub settings — the local coordinator alone is not hosted enforcement.
- Exact-SHA development deployment has executed successfully (automation rehearsal and closeout merge 94d6c59, runtime verification PASS). This does not prove production readiness.
- Provider smoke and reviews now have real executions: structural smoke PASS for minimax (Nebius MiniMax-M3) and glm (Cloudflare); real full reviews executed for the closeout and swap checkpoints through Nebius (MiniMax-M3, Nemotron 3 Ultra) and the OpenCode GLM harness. Kimi smoke and full reviewer tooling/access separation remain unverified. Use the protected EnvironmentFile/operator wrapper method, never loosen /etc/kemirix/agents.env permissions.
- The Nemotron model identifier required correction: the Nebius Token Factory catalog id is `nvidia/Nemotron-3-Ultra-550b-a55b` (the all-lowercase id returns HTTP 404 model-not-found). The VM environment file and the repository's provider default/README now use the corrected id.
- Engineering harness fallbacks are configured (Codex + GPT-6 Astra primary; OpenCode + GLM 5.3 first fallback; OpenCode + MiniMax-M3 via Nebius second fallback, writer minimax) with environment-variable selection and no keys in Git; switching harnesses is a human-approved writer handoff preserving task, branch, base/HEAD SHA, memory, reports and coordinator state. The OpenCode GLM fallback harness has executed (this session) under an operator-directed continuation; the minimax fallback writer has not executed as a writer. The user's normal ChatGPT-authenticated Codex setup must not be broken.
- The Codex task worktree exists; full five-agent worktree initialization/access separation remains unverified. Refresh the prepared dry-run base to an approved main SHA before the external rehearsal.
- Five-agent dry run is prepared, not executed as a single five-worktree rehearsal. No external analysis/review/QA result is fabricated; provider executions in the closeout/swap chains were real single-seat calls from the trusted coordination environment.
- Gated writes/submit enforce ownership, but unrestricted shells under one Unix account can bypass them. Operators must restrict reviewer tools/OS write access, protect governance/runtime paths and stop the old writer before handoff. Human approval flags are attestations, not authentication.
- Canonical lifecycle updates and mirrored reports remain uncommitted control-checkout changes until the owner incorporates them into Git. One control VM is supported; the file lock is not distributed coordination.
- DBeaver verification pending. Existing documentation describes usage but does not confirm inspection.
- Source configs use slugs while SKILL.md uses S01–S27 IDs. SOURCE_STATUS.yaml maps the two; decide persisted identifier/object-key contracts before domain coding.
- SKILL.md names evidence.source_version and evidence.block_subject beyond the shorter config/database table list. Reconcile the schema contract in a reviewed design task before migrations; no domain DDL was changed here.
