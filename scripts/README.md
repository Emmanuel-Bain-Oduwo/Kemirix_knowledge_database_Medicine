# Scripts

Architecture placeholders only. Future entry points:

- `ingest_source.py` — acquire/cache one configured source.
- `resolve_kmx.py` — run deterministic KMX resolution.
- `build_rules.py` — compile approved executable Evidence into Rules.
